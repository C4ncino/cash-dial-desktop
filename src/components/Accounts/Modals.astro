---
import { Modal } from "webcoreui/astro";

import Form from "./Form";
import { MODAL_ID } from "@/types/enums";
---

<Modal id={MODAL_ID.ACCOUNT.CREATE} title="Crear Cuenta" className="glass-elevated w-[calc(100vw-2rem)]! max-w-lg! max-h-[calc(100dvh-2rem)] overflow-y-auto overscroll-contain backdrop-blur-md">
    <Form client:visible modalId={MODAL_ID.ACCOUNT.CREATE} />
</Modal>

<Modal id={MODAL_ID.ACCOUNT.EDIT} title="Editar Cuenta" className="glass-elevated w-[calc(100vw-2rem)]! max-w-lg! max-h-[calc(100dvh-2rem)] overflow-y-auto overscroll-contain backdrop-blur-md">
    <Form client:visible modalId={MODAL_ID.ACCOUNT.EDIT} />
</Modal>

<script>
    import { MODAL_ID } from "@/types/enums";
    import { modal } from "webcoreui";

    document.addEventListener("astro:page-load", () => {
        modal({
            trigger: "#create-account-button",
            modal: `#${MODAL_ID.ACCOUNT.CREATE}`,
        });

        modal({
            trigger: "#edit-account-button",
            modal: `#${MODAL_ID.ACCOUNT.EDIT}`,
        });
    });
</script>
