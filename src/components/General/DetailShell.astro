<div
  class="mx-auto grid w-full max-w-3xl min-w-0 gap-4 sm:grid-cols-[minmax(0,1fr)_auto] sm:items-start sm:gap-6"
>
  <article class="min-w-0">
    <slot />
  </article>
  <aside
    class="order-first sm:order-0 flex flex-wrap items-center gap-2 sm:sticky sm:top-6 sm:flex-col sm:items-stretch"
  >
    <slot name="actions" />
  </aside>
</div>
